import InventoryTable from "../components/InventoryTable";

export default function TablesPage() {
  return (
    <InventoryTable
      itemType="TABLE"
      title="Tables Inventory"
      description="Manage and track tables across all branches."
    />
  );
}